// Stub for the `server-only` package in test environments.
// The real package throws when imported outside a Next.js server context.
// This empty module allows test files to import server-only modules safely.
export {};
