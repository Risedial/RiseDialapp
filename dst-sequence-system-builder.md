# DST Prompt Sequence: Autonomous System Builder Command
> Generated by /dst:help · 2026-05-05
> Paste each command into Claude Code in the order shown.
> Each step tells you whether to open a fresh chat or use your current active chat.

---

## How This Works
Each command outputs a file. The next command reads that file.
You do not need to keep these steps in the same chat — the files carry the context.

---

## What You're Building

A self-contained slash command (with its own reference folder) that:
- Accepts any user objective — inline text, file paths, folder paths, or a mix
- Uses AskUserQuestion-style clarification (modeled after prep-vision.md) to get 100% aligned on the user's vision before building anything
- Spawns non-overlapping sub-agents — each with a single bounded task — to design and generate a complete autonomous orchestration system for that objective
- Produces: `state.json`, context files, step plans, COLLECT/PLAN/EXECUTE prompt files, and a runner slash command — the full architecture

---

## Step 1 of 3 — Observe → SOT

> ⚡ **ACTIVE CHAT** — Run this in the chat where you built the RiseDial orchestration system.
> observe needs access to the orchestration files, bootstrap methodology, and session artifacts created in this session.

**Copy and paste this exactly:**
```
/dst:observe session
```

**What this produces:** `autonomous-system-builder-sot.md` saved in your working directory.
> ⚠️ **If a file with that name already exists**, `/dst:observe` will auto-increment rather than overwrite — the actual file will be `autonomous-system-builder-sot-2.md` (or `-3`, etc.). Check what was actually written before copying the name to Step 2.

**What observe will extract from this session:**
- The slash command + state.json control loop pattern
- The COLLECT / PLAN / EXECUTE mode separation principle
- Context files as anti-hallucination anchors
- Prompt files as self-contained step instructions with hard constraints
- The bootstrap → runner architecture (one-time setup + resumable per-session loop)
- Sub-agent isolation pattern (each agent: bounded task, no overlap, state sync)
- Idempotency via state.json (skip completed, resume from last pending)
- The prep-vision.md clarification style (AskUserQuestion-driven vision alignment)

**Before Step 2:** Confirm the exact filename that was created.

---

## Step 2 of 3 — Blueprint → Meta-Prompt

> 🆕 **FRESH CHAT** — Open a new Claude Code session.
> The SOT is a self-contained file. No active session context is needed.
> Confirm `autonomous-system-builder-sot.md` exists in your working directory before running.

**Copy and paste this exactly:**
```
/dst:blueprint autonomous-system-builder-sot.md "a self-contained slash command with a reference folder that accepts any user objective as inline text, file paths, or folder paths, uses AskUserQuestion-style clarification modeled after prep-vision.md to fully align on the user vision before building, then spawns non-overlapping sub-agents to generate a complete autonomous orchestration system including state.json, context files, step plans, COLLECT/PLAN/EXECUTE prompt files, and a runner slash command for any objective whether ongoing or end-state"
```

**What this produces:** `system-builder-command-metaprompt.md` saved in your working directory.
> ⚠️ **If a file with that name already exists**, `/dst:blueprint` will auto-increment — the actual file will be `system-builder-command-metaprompt-2.md` (or `-3`, etc.). Check what was actually written before copying the name to Step 3.

**Before Step 3:** Confirm the exact filename that was created.

---

## Step 3 of 3 — Mint → Final Artifact

> 🆕 **FRESH CHAT** — Open a new Claude Code session.
> The meta-prompt is self-contained by design. Pure execution — no prior context needed.
> Confirm `system-builder-command-metaprompt.md` exists in your working directory before running.

**Copy and paste this exactly:**
```
/dst:mint system-builder-command-metaprompt.md
```

**What this produces:** A new Claude Code slash command saved to a self-contained folder in `~/.claude/commands/` that accepts any user objective (inline or via file/folder paths), runs vision-alignment clarification questions, then uses non-overlapping sub-agents to build a complete autonomous orchestration system (state.json, context files, step plans, COLLECT/PLAN/EXECUTE prompt files, and a runner slash command) — ready to execute for any objective, ongoing or end-state.

---

## If Something Goes Wrong
- **Step 1 fails:** Make sure you're in the active chat where the RiseDial system was built — not a fresh chat.
- **Step 2 can't find the SOT:** Check your working directory matches where Step 1 ran (`C:\Users\Alexb\Documents\RiseDialapp`).
- **Step 3 hits an escalation:** Follow the on-screen prompts — mint will ask you to decide.
- **Want to refine the SOT before blueprinting:** Re-run Step 1 and add additional file paths after `session` space-separated to merge more context.
