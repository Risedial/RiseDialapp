import { test } from '@playwright/test';

// Placeholder — real e2e tests are added in M5-C and later milestones.
// This file exists so `playwright test --list` exits 0 on an otherwise empty suite.
test.skip('placeholder', () => {});
