# Step 11: M5-A — Test Infrastructure: Install packages and update package.json

## Hard Constraints (apply to every action in this step)
1. Output file size MUST NOT exceed 32,000 tokens. If output would exceed this limit, split into multiple files and create a follow-up step.
2. Do not truncate any file. Write each file completely or not at all.
3. After completing this step, update C:\Users\Alexb\Documents\RiseDialapp\vision-risedial-01-Build\orchestration\state.json: move "prompt-11" from pendingSteps to completedSteps and set its status to "complete".
4. Do not install or import any package not already present in package.json.
5. Use the Write tool only. Do not use Edit on files that do not yet exist.

## Prerequisites
State: `C:\Users\Alexb\Documents\RiseDialapp\vision-risedial-01-Build\orchestration\state.json` — pendingSteps must contain "prompt-11"
Context files (read these before executing — they contain values you must not invent):
- C:\Users\Alexb\Documents\RiseDialapp\vision-risedial-01-Build\context\external-services.md

## Task

Install Vitest, Playwright, and all required test devDependencies. Update `package.json` with test scripts and new devDependencies. Regenerate `package-lock.json`.

Key values from context (do NOT invent):
- `package_scripts:test`: `vitest run` (from external-services.md)
- `package_scripts:test:watch`: `vitest` (from external-services.md)
- `package_scripts:test:coverage`: `vitest run --coverage` (from external-services.md)
- `package_scripts:test:e2e`: `playwright test` (from external-services.md)
- `vitest_coverage_provider`: `@vitest/coverage-v8` (from external-services.md)
- `playwright_browser`: Chromium (Desktop Chrome) (from external-services.md)
- `gitignore_entries` to add: `.env.local, coverage/, playwright-report/, test-results/, e2e/fixtures/` (from external-services.md)

**Sub-step 1 — Read the current package.json:**
Read `C:\Users\Alexb\Documents\RiseDialapp\package.json` in full to understand its current state.

**Sub-step 2 — Write the updated package.json:**
Write `C:\Users\Alexb\Documents\RiseDialapp\package.json` with:
- All existing dependencies and devDependencies preserved
- `jsonwebtoken` and `@types/jsonwebtoken` removed (already done in prompt-09, just ensure they are absent)
- Four test scripts added: `"test": "vitest run"`, `"test:watch": "vitest"`, `"test:coverage": "vitest run --coverage"`, `"test:e2e": "playwright test"`
- New devDependencies added:
  - `"@playwright/test": "^1.44.0"`
  - `"@testing-library/jest-dom": "^6.4.6"`
  - `"@vitejs/plugin-react": "^4.3.0"`
  - `"@vitest/coverage-v8": "^1.6.0"`
  - `"jsdom": "^24.1.0"`
  - `"vitest": "^1.6.0"`

**Sub-step 3 — Run npm install:**
Run: `npm install`
If it fails with peer dependency conflicts, retry with: `npm install --legacy-peer-deps`
Do NOT hand-edit `package-lock.json`.

**Sub-step 4 — Install Playwright browsers:**
Run: `npx playwright install chromium`

**Sub-step 5 — Update .gitignore:**
Read `C:\Users\Alexb\Documents\RiseDialapp\.gitignore`. If any of these entries are missing, add them:
```
coverage/
playwright-report/
test-results/
e2e/fixtures/
```
Do NOT remove any existing entries.

**Sub-step 6 — Type check:**
Run `npx tsc --noEmit`. It must exit 0 before committing.

**Sub-step 7 — Commit:**
Stage: `git add package.json package-lock.json .gitignore`
Commit message: `chore(test): install vitest, playwright, and coverage dependencies`

## Verification
- [ ] `package.json` contains scripts: `"test": "vitest run"`, `"test:watch": "vitest"`, `"test:coverage": "vitest run --coverage"`, `"test:e2e": "playwright test"`
- [ ] `package.json` devDependencies includes `vitest`, `@vitest/coverage-v8`, `@vitejs/plugin-react`, `@playwright/test`, `@testing-library/jest-dom`, `jsdom`
- [ ] `package-lock.json` was regenerated by `npm install`
- [ ] `.gitignore` contains `coverage/`, `playwright-report/`, `test-results/`, `e2e/fixtures/`
- [ ] `npx tsc --noEmit` exits 0
- [ ] `git log --oneline` shows a new commit with message containing `chore(test)`

## State Update
In `C:\Users\Alexb\Documents\RiseDialapp\vision-risedial-01-Build\orchestration\state.json`:
- Move "prompt-11" from pendingSteps to completedSteps
- Set steps["prompt-11"].status = "complete"
