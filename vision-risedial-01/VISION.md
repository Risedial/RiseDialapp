# RiseDial — Codebase Audit, Fix & Test Pipeline Vision

<!-- METADATA -->
> **Domain:** Software product  
> **Builder:** Claude Code / AI Agent  
> **Date:** 2026-05-04  
> **Status:** Ready for /vision-to-docs  

---

## 1. PROJECT SUMMARY

### 1.1 What This Builds
A systematic audit, fix, and test pipeline that takes the RiseDial Next.js 14 App Router codebase from its current state — AI-generated, never locally validated, surfacing build errors one at a time in Vercel — to a fully passing build with zero TypeScript errors, all 14 structural concerns audited and corrected, and a complete Vitest + Playwright test suite covering 100% of `lib/` and all API routes.

### 1.2 The Problem It Solves
The RiseDial codebase was generated by 48 sequential AI orchestration prompts and pushed directly to GitHub without ever being compiled or tested locally. Every bug surfaces only when Vercel's build pipeline hits it — one at a time — making forward progress impossible to predict and shipping to real users impossible to trust. Developers cannot confidently deploy because no test suite, no type-clean build, and no verified end-to-end flow exists.

### 1.3 Who Uses It
The developer (Alex) who owns the RiseDial product. They use the pipeline output — a clean build, corrected code, and a passing test suite — to confidently deploy RiseDial and serve real paying users.

### 1.4 End-to-End Success Signal
The system is working when: `npx tsc --noEmit` exits 0, `next build` succeeds on Vercel, `npx vitest run` exits 0 with 100% line coverage of all files in `lib/` and `app/api/`, and `npx playwright test` exits 0 for all four E2E flows — all in a single CI run on GitHub Actions.

---

## 2. SYSTEM OVERVIEW

### 2.1 Module List
| # | Module Name | One-Line Description |
|---|-------------|----------------------|
| 1 | Build Error Resolution | Eliminate all TypeScript and `next build` blockers before any other work |
| 2 | Codebase Audit | Systematic 14-area review of every structural and correctness concern |
| 3 | Code Fixes | Apply all changes identified by the audit — including the webhook consolidation |
| 4 | Environment Variable Validation | Add `lib/env.ts` with zod that throws at module-load time if any required env var is absent |
| 5 | Test Infrastructure | Install and configure Vitest + Playwright; add npm test scripts |
| 6 | Unit & Integration Test Suite | Write Vitest tests achieving 100% line coverage of `lib/` and `app/api/` |
| 7 | E2E Test Suite | Write Playwright tests covering the four critical user flows against a local dev server |
| 8 | CI Pipeline | Add `.github/workflows/ci.yml` running tsc + Vitest + Playwright on every push and PR |

### 2.2 System Boundary

**Included in this build:**
- Fixing all TypeScript type errors and build blockers
- Auditing and correcting all 14 concern areas listed in Section 3.2
- Consolidating duplicated webhook handler logic
- Adding env-var startup validation
- Full Vitest + Playwright test suite
- GitHub Actions CI workflow

**Explicitly NOT included (out of scope):**
- New product features or UI changes
- Database migrations (schema is accepted as-is from `001_initial_schema.sql`)
- Supabase RLS policy redesign (RLS policies exist but are bypassed by the service role client; documenting this as a known risk but not rewriting policies)
- Stripe product/price ID configuration (IDs stored as env vars, not changed)
- OpenAI prompt engineering changes

**External dependencies (already exist, not built here):**
| System | What It Provides | Required By Module |
|--------|-----------------|-------------------|
| Supabase (cloud project) | PostgreSQL database via service role client | 2, 3, 6 |
| Stripe (test + live mode) | Subscription billing, webhooks | 2, 3, 6, 7 |
| OpenAI API | gpt-4o-mini (chat), gpt-4o (memory compression) | 2, 6 |
| Vercel | Build pipeline, deployment hosting | 1, 8 |
| GitHub | Source control, CI runner | 8 |

---

## 3. MODULE SPECIFICATIONS

### 3.1 Module 1 — Build Error Resolution

**Purpose:** Produce a clean `tsc --noEmit` and `next build` before any other module runs.

**Trigger:**
- Type: Manual
- Specifics: Developer runs `npx tsc --noEmit` and `next build` locally; failure output identifies what to fix

**Inputs:**
| Field | Type | Source | Required? | Constraints |
|-------|------|--------|-----------|-------------|
| Current TypeScript errors | text output | `npx tsc --noEmit` | Yes | Must list all errors — use `--noEmit` not build |
| `tsconfig.tsbuildinfo` | file | project root | Yes | Likely stale; file is in git untracked list |

**Outputs:**
| Field | Type | Destination | Format / Example |
|-------|------|-------------|-----------------|
| Zero-error tsc run | exit code 0 | terminal | `npx tsc --noEmit` exits with code 0, no output |
| Passing next build | Vercel build log | Vercel dashboard | Build completes without error |

**Step-by-step process:**
1. Delete `tsconfig.tsbuildinfo` from the project root. This file is a stale incremental build cache; its presence is the most likely cause of the phantom `Cannot find type definition file for 'testing-library__jest-dom'` error. TypeScript's `skipLibCheck: true` is already set in `tsconfig.json`, which should prevent this error unless the tsbuildinfo is pointing to a cached (now-removed) type entry.
2. Run `npx tsc --noEmit` and capture all remaining errors.
3. For each error, apply the minimal fix. Do not refactor surrounding code.
4. If the `@types/testing-library__jest-dom` error persists after deleting `tsconfig.tsbuildinfo`:
   - If: `node_modules/@types/testing-library__jest-dom` exists → the package was installed by a transitive dependency. Add `"types": ["node", "react", "react-dom"]` to `tsconfig.json` `compilerOptions` to restrict auto-discovery.
   - If: the package does not exist → run `npm install --save-dev @types/testing-library__jest-dom` to satisfy TypeScript's auto-discovery.
5. Run `npx tsc --noEmit` again. Repeat steps 3–4 until exit code is 0.
6. Run `next build` locally. Capture any additional build errors not caught by tsc (e.g., invalid App Router exports).
7. Fix any `next build` errors. The `export const config` Pages Router syntax error in `app/api/webhooks/stripe/route.ts` was already fixed in the prior session.
8. Commit when `tsc --noEmit` and `next build` both exit 0.

**Edge cases:**
| Scenario | System Behavior | User Notified? | Processing Continues? |
|----------|----------------|----------------|----------------------|
| Deleting tsbuildinfo causes new errors to appear | Those were pre-existing masked errors; fix them | No | Yes |
| `next build` fails with a different error than tsc | Separate category of error; investigate and fix independently | No | Yes |
| `jsonwebtoken` still imported in a file other than middleware | TypeScript error will surface; remove import and replace with `verifySession` from `lib/auth/session.ts` | No | Yes |

**Failure states:**
| Failure Type | Recovery Action | Data Impact |
|-------------|----------------|-------------|
| tsc errors cannot be resolved in isolation | Escalate to Module 2 audit; some errors may require understanding the full call chain | None |

**AI/LLM used:** No

**External integrations:** None

**Data stored:** None

---

### 3.2 Module 2 — Codebase Audit

**Purpose:** Perform a systematic read-only review of all 14 concern areas and produce a structured findings document before touching any additional code.

**Trigger:**
- Type: Module hand-off
- Specifics: Triggered after Module 1 (build passes). Read-only — no code changes during this module.

**Inputs:**
| Field | Type | Source | Required? | Constraints |
|-------|------|--------|-----------|-------------|
| All source files in `lib/`, `app/`, `middleware.ts` | TypeScript files | Local filesystem | Yes | |
| `supabase/migrations/001_initial_schema.sql` | SQL file | Local filesystem | Yes | Ground truth for schema |
| `package.json` | JSON | Local filesystem | Yes | |
| `tsconfig.json` | JSON | Local filesystem | Yes | |
| `public/manifest.json` | JSON | Local filesystem | Yes | PWA manifest |
| `public/sw.js` | JavaScript | Local filesystem | Yes | Service worker |

**Outputs:**
| Field | Type | Destination | Format / Example |
|-------|------|-------------|-----------------|
| `AUDIT.md` | Markdown file | Project root | Structured findings per concern area (see format below) |

**Audit areas — step-by-step:**

**Area 1 — Import and module resolution.**
1. For every `import` statement in `lib/`, `app/`, and `middleware.ts`: verify the imported path exists as a file and the named export exists with the exact name used.
2. Check specifically: `lib/openai/client.ts` exports `callRise` and `callCompression`; `lib/memory/executor.ts` exports `executeCompressionAsync`; `lib/supabase/server.ts` exports `supabaseServer` (singleton, not a function); `lib/rise/rate-limit.ts` exports `checkRateLimit` and `recordMessage`.
3. Flag any import that resolves to a non-existent path or non-existent named export.

**Area 2 — TypeScript strict type correctness.**
1. Verify `tsc --noEmit --strict` exits 0 (strict mode is already on in tsconfig.json).
2. Check Stripe SDK v22 type shapes: `Stripe.Invoice.customer` is `string | Stripe.Customer | Stripe.DeletedCustomer | null` — all casts must handle the full union.
3. Check `Uint8Array.buffer as ArrayBuffer` cast in `lib/auth/session.ts:62` is correct.
4. Check all `supabaseServer.from('table').select()` return types are properly narrowed.

**Area 3 — Runtime environment compatibility.**
1. List every file that `middleware.ts` imports (direct and transitive).
2. Verify none of those files import `bcryptjs`, `jsonwebtoken`, `fs`, `path`, `crypto` (Node built-in module, not Web Crypto API), or any package that requires Node.js runtime.
3. The `verifySession` function in `lib/auth/session.ts` uses only `crypto.subtle` — this is correct for Edge Runtime. Confirm no other Edge-imported file uses Node-only APIs.
4. All files under `app/api/` (except the webhook route, which already excludes the `runtime = 'edge'` declaration) run in Node.js runtime — confirm by checking for `export const runtime = 'edge'` declarations.
5. Check `lib/memory/executor.ts` line 1: `import 'server-only'` — this correctly prevents Edge Runtime use.

**Area 4 — Stripe integration correctness.**
1. Confirm Stripe SDK version is `^22.1.0` (confirmed in package.json).
2. Confirm `apiVersion` in `lib/stripe/config.ts` is `'2026-04-22.dahlia'`.
3. Audit ALL uses of `subscription.current_period_end` across the entire codebase — must be zero occurrences. All uses must be `subscription.items.data[0].current_period_end`.
4. **Webhook duplication finding:** `app/api/webhooks/stripe/route.ts` contains a complete inline implementation of all four event handlers (`checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.payment_failed`) that duplicates `lib/stripe/webhooks.ts`. The route file does NOT import `routeWebhookEvent` from the lib file. The lib file's `routeWebhookEvent` export is dead code (never called). The route file has idempotency logic (webhook_events table check) that the lib file lacks. This duplication must be consolidated (see Module 3).
5. **Dead code finding:** `const PREMIUM_PRODUCT_ID = 'prod_URPiU0OHsZuXvk'` in the route file is declared but never referenced. Must be deleted.
6. Verify idempotency pattern: route file inserts into `webhook_events` BEFORE processing (crash-safe pre-insert). Confirm this pattern is preserved after consolidation.

**Area 5 — Supabase schema validation.**
1. For every `supabaseServer.from('table_name')` call across the codebase, verify `table_name` matches exactly one of: `users`, `chats`, `messages`, `memory_profiles`, `rate_limit_tracking`, `webhook_events`.
2. For every `.select('field')`, `.update({ field: value })`, and `.insert({ field: value })`: verify `field` exists as a column in the corresponding table per `001_initial_schema.sql`.
3. **RLS risk finding:** The migration enables RLS on all tables with policies using `auth.uid()` (Supabase's built-in auth system). The app uses a custom HS256 JWT and the `supabaseServer` service role client, which bypasses RLS entirely. The RLS policies are effectively dead code — they provide no protection and no access for browser-side queries. Document this in `AUDIT.md` as a security architecture note. Out of scope to fix; in scope to document.
4. Verify `memory_profiles` has a `UNIQUE` constraint on `user_id` (it does — confirmed in migration). This is required for the upsert pattern the compression code uses.

**Area 6 — Authentication flow.**
1. Trace the full sign-up flow: user submits email/password → password hashed with bcryptjs → user row inserted → `createSession(userId, subscriptionStatus)` called → cookie set via `setSessionCookie`.
2. Trace the full login flow: email/password submitted → `users` table query → bcryptjs compare → session created → cookie set.
3. Verify `middleware.ts` matcher covers all API routes that require authentication. Current matcher: `/api/chat/:path*`, `/api/memory/:path*`, `/api/subscription/:path*`, `/api/chats/:path*`, `/api/user/:path*`. Verify no authenticated route exists outside these paths.
4. **Security finding:** `lib/auth/session.ts` line 3: `const JWT_SECRET = process.env.JWT_SECRET ?? "changeme-insecure-fallback"`. If `JWT_SECRET` is not set in the environment, tokens are signed with a known public string. This is a critical security risk. Module 4 (env validation) must ensure `JWT_SECRET` throws on missing.
5. Verify `setSessionCookie` sets `httpOnly: true`, `sameSite: 'strict'`, `secure: true` — confirmed in current code.
6. Verify `verifySession` checks `exp` claim and returns `null` on expired tokens — confirmed.
7. Identify all API routes that read `x-user-id` and `x-subscription-status` headers set by middleware. Verify these routes do not have a fallback if the headers are absent (they shouldn't, since middleware blocks unauthenticated requests before they reach the route).

**Area 7 — Subscription flow.**
1. Trace the checkout creation flow: user selects plan → checkout session created with `metadata.user_id` → user redirected to Stripe.
2. Trace the webhook activation flow: Stripe fires `checkout.session.completed` → webhook route verifies signature → idempotency check → subscription retrieved with `expand: ['items.data.price']` → user record updated with `subscription_status: 'active'`, plan type, billing date, premium flag.
3. Verify `lib/auth/subscription-gate.ts` exists and is actually imported by every route that serves gated content. List any routes that serve premium content but do not call the subscription gate.
4. Verify the subscription gate reads `x-subscription-status` from the request header (set by middleware) and returns 402 or 403 for non-active subscriptions.

**Area 8 — Memory compression pipeline.**
1. Verify `user_message_index` is set on every user message insert. If messages are inserted without `user_message_index`, `checkCompressionTrigger` will never count them (it filters `.not('user_message_index', 'is', null)`), and compression will never fire.
2. Confirm trigger thresholds: initial compression fires at exactly 50 user messages in a chat; patch compression fires every 10 messages thereafter (at 60, 70, 80...).
3. Confirm `executeCompressionAsync` is called with `void` (fire-and-forget) in the message route so it does not block the HTTP response.
4. Verify `generateInitialProfile` and `patchMemoryProfile` both call OpenAI with JSON mode and parse the response correctly.
5. Verify `memory_profiles` upsert uses `user_id` as the conflict target (per the `UNIQUE` constraint).

**Area 9 — Rate limiting.**
1. **Race condition finding:** `recordMessage` reads the active window's `message_count`, increments it in application code, then writes back. Under concurrent requests (user sending messages quickly), two simultaneous requests can both read the same `message_count`, both compute the same incremented value, and both write it — effectively under-counting. Document this in `AUDIT.md`. Fix requires an atomic increment: either a Supabase RPC function or a raw SQL UPDATE with `message_count = message_count + 1`.
2. Verify the rolling window cutoff logic: `window_start >= now() - 60 minutes`. Confirm this is `gte` (greater-than-or-equal), not `gt`.
3. Verify `checkRateLimit` returns `{ allowed: boolean, remaining: number }` and the message route destructures it as `const { allowed: rateLimitAllowed }` (this fix was already applied).

**Area 10 — Dead code and duplication.**
1. Flag `const PREMIUM_PRODUCT_ID = 'prod_URPiU0OHsZuXvk'` in `app/api/webhooks/stripe/route.ts` — unused, must be deleted.
2. Flag `routeWebhookEvent` export in `lib/stripe/webhooks.ts` — never imported by the route. After webhook consolidation (Module 3), this becomes the primary export.
3. Check whether `jsonwebtoken` is still imported anywhere after the middleware fix. If not imported anywhere, remove it from `package.json` dependencies.
4. Check whether `@types/jsonwebtoken` in devDependencies can also be removed.
5. Identify any other files in `orchestration/` or elsewhere that are not part of the app runtime and should be excluded from TypeScript compilation via `tsconfig.json` `exclude` field if they cause type errors.

**Area 11 — Environment variable audit.**
1. Enumerate every `process.env.X` reference in the entire codebase. Expected complete list:
   - `SUPABASE_URL` — Supabase project URL
   - `SUPABASE_SERVICE_ROLE_KEY` — Supabase service role key
   - `JWT_SECRET` — HS256 signing secret (critical: must not have fallback)
   - `STRIPE_SECRET_KEY` — Stripe secret key
   - `STRIPE_WEBHOOK_SECRET` — Stripe webhook signing secret
   - `STRIPE_PRICE_MONTHLY` — Stripe Price ID for monthly base plan
   - `STRIPE_PRICE_ANNUAL` — Stripe Price ID for annual base plan
   - `STRIPE_PRICE_PREMIUM_MONTHLY_ADDON` — Stripe Price ID for premium memory monthly add-on
   - `STRIPE_PRICE_PREMIUM_ANNUAL_ADDON` — Stripe Price ID for premium memory annual add-on
   - `OPENAI_API_KEY` — OpenAI API key
   - Any `NEXT_PUBLIC_*` vars used client-side
2. Verify this list is complete by grepping for `process.env.` across all files.
3. Flag any `process.env.X!` (non-null assertion without validation) — these will throw at runtime with an unhelpful error if the var is missing.

**Area 12 — PWA correctness.**
1. Verify `public/manifest.json` has required fields: `name`, `short_name`, `start_url`, `display`, `background_color`, `theme_color`, `icons` (with at least one 192×192 and one 512×512 PNG).
2. Verify the icon PNG files referenced in `manifest.json` exist in `public/`.
3. Verify `next-pwa` configuration in `next.config.js` (or `next.config.ts`): `dest: 'public'`, `register: true`, `skipWaiting: true`.
4. Verify `public/sw.js` is not empty and is a valid Workbox service worker (the file exists per git status).
5. Verify the app's root layout includes `<link rel="manifest" href="/manifest.json" />` and `<meta name="theme-color" ... />`.

**Area 13 — Test strategy (documented, not implemented in this module).**
Document in `AUDIT.md` the test plan that Modules 5–7 will implement:
- Unit tests: pure functions in `lib/auth/session.ts`, `lib/rise/rate-limit.ts`, `lib/memory/trigger.ts`, `lib/stripe/webhooks.ts`
- Integration tests: API route handlers with mocked `supabaseServer` and `stripe` client
- E2E tests: 4 Playwright flows listed in Module 7

**Area 14 — Fix execution plan.**
The final section of `AUDIT.md` is an ordered list of every fix to apply in Module 3, sequenced so each fix can be committed independently without breaking other files.

**Edge cases:**
| Scenario | System Behavior | User Notified? | Processing Continues? |
|----------|----------------|----------------|----------------------|
| A file referenced in an import does not exist | Log in AUDIT.md under Area 1 as a blocking finding | No | Yes — continue auditing other areas |
| A column referenced in code does not exist in the migration | Log as a blocking finding; note which table and column | No | Yes |
| `lib/auth/subscription-gate.ts` does not exist | Log as a blocking finding under Area 7; note which routes are unprotected | No | Yes |

**Failure states:**
| Failure Type | Recovery Action | Data Impact |
|-------------|----------------|-------------|
| More than 20 distinct findings in AUDIT.md | Group findings by severity (blocking vs warning) before passing to Module 3 | None |

**AI/LLM used:** No

**Data stored:**
| Field | Type | Storage Location | Retention Period |
|-------|------|-----------------|-----------------|
| AUDIT.md findings | Markdown file | Project root | Until project is complete |

---

### 3.3 Module 3 — Code Fixes

**Purpose:** Apply every fix identified in `AUDIT.md` in dependency order, with one atomic git commit per fix.

**Trigger:**
- Type: Module hand-off
- Specifics: `AUDIT.md` is complete; each fix from Section 14 of `AUDIT.md` is executed in sequence

**Inputs:**
| Field | Type | Source | Required? | Constraints |
|-------|------|--------|-----------|-------------|
| AUDIT.md | Markdown | Project root | Yes | All 14 areas must be complete |
| Source files identified in AUDIT.md | TypeScript/JS | Local filesystem | Yes | |

**Outputs:**
| Field | Type | Destination | Format / Example |
|-------|------|-------------|-----------------|
| Corrected source files | TypeScript | `lib/`, `app/api/`, `middleware.ts` | Each fix committed independently |
| Consolidated webhook handler | TypeScript | `lib/stripe/webhooks.ts` | Single source of truth for all 4 event types + idempotency |

**Step-by-step process (webhook consolidation — the most complex fix):**
1. Move the idempotency check logic (webhook_events insert/lookup) from `app/api/webhooks/stripe/route.ts` into `lib/stripe/webhooks.ts` as a wrapper around `routeWebhookEvent`. The route file becomes a thin dispatcher: parse body, verify signature, call `lib/stripe/webhooks.ts` dispatcher.
2. Delete the inline handler functions (`getPlanTypeFromPriceId`, `detectPremiumItem`, `getBasePriceId`, `handleCheckoutSessionCompleted`, etc.) from the route file.
3. Delete `const PREMIUM_PRODUCT_ID = 'prod_URPiU0OHsZuXvk'`.
4. Verify the consolidated `lib/stripe/webhooks.ts` handles all 4 events, includes idempotency logic, and exports: `verifyWebhookSignature`, `routeWebhookEvent` (now includes idempotency).

**Step-by-step process (rate limiter atomicity fix):**
1. Replace the read-then-increment-then-write pattern in `recordMessage` with an atomic Supabase RPC call.
2. Create a Supabase database function `increment_message_count(p_user_id uuid)` that performs an atomic `UPDATE rate_limit_tracking SET message_count = message_count + 1 WHERE id = (SELECT id FROM rate_limit_tracking WHERE user_id = p_user_id AND window_start >= now() - interval '60 minutes' ORDER BY window_start DESC LIMIT 1)`. If no rows updated, insert a new window record.
3. Call this RPC from `recordMessage` via `supabaseServer.rpc('increment_message_count', { p_user_id: userId })`.

**Step-by-step process (JWT_SECRET security fix):**
1. In `lib/auth/session.ts`, remove the `?? "changeme-insecure-fallback"` fallback.
2. Change to: `const JWT_SECRET = process.env.JWT_SECRET` (bare, no fallback).
3. After Module 4 (env validation) adds `lib/env.ts`, import `JWT_SECRET` from there instead of reading `process.env` directly.

**Step-by-step process (all other fixes):**
- Each fix from AUDIT.md Areas 1–14 is applied as a separate commit with a message describing the area and the specific change.

**Edge cases:**
| Scenario | System Behavior | User Notified? | Processing Continues? |
|----------|----------------|----------------|----------------------|
| Webhook consolidation breaks the route's type signatures | Fix the type errors before committing | No | Yes |
| Supabase RPC for atomic increment requires a migration | Create migration file `supabase/migrations/002_atomic_rate_limit.sql` | No | Yes |

**AI/LLM used:** No

**Data stored:** None (code changes only, committed to git)

---

### 3.4 Module 4 — Environment Variable Validation

**Purpose:** Add `lib/env.ts` that validates all required environment variables at module-load time using zod, throwing a descriptive error if any are absent or malformed.

**Trigger:**
- Type: Module hand-off
- Specifics: Triggered after Module 3 (all code fixes applied). `zod` is already in `package.json` dependencies.

**Inputs:**
| Field | Type | Source | Required? | Constraints |
|-------|------|--------|-----------|-------------|
| Complete env var list from AUDIT.md Area 11 | list | AUDIT.md | Yes | |
| `package.json` | JSON | project root | Yes | zod must be present |

**Outputs:**
| Field | Type | Destination | Format / Example |
|-------|------|-------------|-----------------|
| `lib/env.ts` | TypeScript module | `lib/env.ts` | Exports typed env object; throws `Error` with env var name if missing |
| `.env.example` | text file | project root | Lists every required env var with a placeholder value |

**Step-by-step process:**
1. Create `lib/env.ts` using zod:
```typescript
import { z } from 'zod'

const envSchema = z.object({
  SUPABASE_URL: z.string().url(),
  SUPABASE_SERVICE_ROLE_KEY: z.string().min(1),
  JWT_SECRET: z.string().min(32, 'JWT_SECRET must be at least 32 characters'),
  STRIPE_SECRET_KEY: z.string().startsWith('sk_'),
  STRIPE_WEBHOOK_SECRET: z.string().startsWith('whsec_'),
  STRIPE_PRICE_MONTHLY: z.string().startsWith('price_'),
  STRIPE_PRICE_ANNUAL: z.string().startsWith('price_'),
  STRIPE_PRICE_PREMIUM_MONTHLY_ADDON: z.string().startsWith('price_'),
  STRIPE_PRICE_PREMIUM_ANNUAL_ADDON: z.string().startsWith('price_'),
  OPENAI_API_KEY: z.string().startsWith('sk-'),
})

export const env = envSchema.parse(process.env)
```
2. Update `lib/supabase/server.ts` to import `env.SUPABASE_URL` and `env.SUPABASE_SERVICE_ROLE_KEY` from `lib/env.ts` instead of reading `process.env` directly (and remove the manual `if (!supabaseUrl)` checks, since zod handles this).
3. Update `lib/stripe/config.ts` to import `env.STRIPE_SECRET_KEY`.
4. Update `lib/auth/session.ts` to import `env.JWT_SECRET`.
5. Update `lib/stripe/webhooks.ts` to import `env.STRIPE_WEBHOOK_SECRET`, `env.STRIPE_PRICE_MONTHLY`, etc.
6. Create `.env.example` listing all vars with placeholder values.
7. Verify `.env.example` is committed but `.env.local` (if it exists) is in `.gitignore`.

**Edge cases:**
| Scenario | System Behavior | User Notified? | Processing Continues? |
|----------|----------------|----------------|----------------------|
| `lib/env.ts` causes circular imports | Move `envSchema.parse` call outside any imported module; `lib/env.ts` should have zero imports from the project | No | Yes |
| A `NEXT_PUBLIC_*` var is needed client-side | Add to envSchema but use `z.string().optional()` for client vars that may be undefined server-side | No | Yes |

**AI/LLM used:** No

**Data stored:** None

---

### 3.5 Module 5 — Test Infrastructure

**Purpose:** Install and configure Vitest and Playwright; add `npm test` scripts; confirm test runners execute successfully on an empty test suite.

**Trigger:**
- Type: Module hand-off
- Specifics: After Module 4. Requires a clean `tsc --noEmit` pass.

**Inputs:**
| Field | Type | Source | Required? | Constraints |
|-------|------|--------|-----------|-------------|
| `package.json` | JSON | project root | Yes | |
| `tsconfig.json` | JSON | project root | Yes | |

**Outputs:**
| Field | Type | Destination | Format / Example |
|-------|------|-------------|-----------------|
| `vitest.config.ts` | TypeScript | project root | Configured for Next.js, jsdom environment, path aliases |
| `playwright.config.ts` | TypeScript | project root | Configured for local dev server on port 3000 |
| Updated `package.json` scripts | JSON | project root | `"test": "vitest run"`, `"test:watch": "vitest"`, `"test:e2e": "playwright test"`, `"test:coverage": "vitest run --coverage"` |
| Installed devDependencies | node_modules | local | vitest, @vitest/coverage-v8, @testing-library/react, @testing-library/jest-dom, jsdom, @playwright/test |

**Step-by-step process:**
1. Install devDependencies: `npm install --save-dev vitest @vitest/coverage-v8 @testing-library/react @testing-library/jest-dom jsdom @playwright/test`
2. Create `vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'
import path from 'path'

export default defineConfig({
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./vitest.setup.ts'],
    coverage: {
      provider: 'v8',
      include: ['lib/**/*.ts', 'app/api/**/*.ts'],
      exclude: ['lib/env.ts'],
      thresholds: { lines: 100, functions: 100, branches: 100, statements: 100 },
    },
    alias: {
      '@': path.resolve(__dirname, '.'),
    },
  },
})
```
3. Create `vitest.setup.ts`: `import '@testing-library/jest-dom'`
4. Create `playwright.config.ts`:
```typescript
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
})
```
5. Add test scripts to `package.json`.
6. Run `npx vitest run` — should exit 0 with no tests found.
7. Run `npx playwright install chromium` to install browser binary.
8. Run `npx playwright test` on an empty `e2e/` directory — should exit 0.

**AI/LLM used:** No

**Data stored:** None

---

### 3.6 Module 6 — Unit & Integration Test Suite

**Purpose:** Write Vitest tests covering 100% of lines, branches, and functions in `lib/` and `app/api/`.

**Trigger:**
- Type: Module hand-off
- Specifics: After Module 5 (test infrastructure passes empty run)

**Test isolation strategy:** `vi.mock()` at the module boundary. `supabaseServer` and the Stripe client are mocked in every test file that uses them. No real network calls. No real database.

**Inputs:**
| Field | Type | Source | Required? | Constraints |
|-------|------|--------|-----------|-------------|
| All files in `lib/` and `app/api/` | TypeScript | local filesystem | Yes | |
| `vitest.config.ts` | TypeScript | project root | Yes | |

**Outputs:**
| Field | Type | Destination | Format / Example |
|-------|------|-------------|-----------------|
| Test files | TypeScript | `__tests__/` directory mirroring `lib/` and `app/api/` | e.g., `__tests__/lib/auth/session.test.ts` |
| Coverage report | HTML + JSON | `coverage/` | 100% lines/branches/functions for all included files |

**Required test files and coverage targets:**

**`__tests__/lib/auth/session.test.ts`**
- `createSession`: returns a 3-part JWT string; payload contains `user_id`, `subscription_status`, `iat`, `exp`; `exp` is 30 days from now
- `verifySession`: returns `{ user_id, subscription_status }` for a valid token; returns `null` for an expired token; returns `null` for a tampered signature; returns `null` for a token with fewer than 3 parts
- `setSessionCookie`: sets httpOnly, sameSite strict, secure, 30-day maxAge, path `/`
- `clearSessionCookie`: sets maxAge 0

**`__tests__/lib/rise/rate-limit.test.ts`**
- `checkRateLimit`: returns `{ allowed: true, remaining: 60 }` when no active window exists; returns `{ allowed: true, remaining: N }` when window.message_count < 60; returns `{ allowed: false, remaining: 0 }` when window.message_count >= 60
- `recordMessage`: calls `supabaseServer.rpc('increment_message_count')` (after the atomic fix); inserts a new window row when no active window exists; increments existing window when one exists

**`__tests__/lib/memory/trigger.test.ts`**
- `checkCompressionTrigger`: returns `{ shouldCompress: true, isInitial: true, isPatch: false }` at exactly 50 user messages; returns `{ shouldCompress: true, isInitial: false, isPatch: true }` at 60, 70, 80; returns `{ shouldCompress: false, ... }` at 49, 51, 59, 61; handles Supabase error by returning `{ shouldCompress: false, ... }`

**`__tests__/lib/memory/executor.test.ts`**
- `executeCompressionAsync`: does not throw under any input; calls `generateInitialProfile` when `isInitial: true`; calls `patchMemoryProfile` when `isPatch: true`; retries up to 3 times on failure; does not retry when trigger returns `shouldCompress: false`

**`__tests__/lib/stripe/webhooks.test.ts`**
- `verifyWebhookSignature`: calls `stripe.webhooks.constructEvent` with correct args; propagates the thrown error on invalid signature
- `routeWebhookEvent`: calls the correct handler for each of the 4 event types; silently ignores unknown event types; idempotency check returns early if `webhook_events` row already exists for the event ID

**`__tests__/app/api/chat/[chatId]/message/route.test.ts`**
- Returns 401 when `x-user-id` header is absent (middleware would have blocked, but test the route in isolation)
- Returns 429 when `checkRateLimit` returns `{ allowed: false }`
- Returns 200 with assistant message when all checks pass
- Calls `executeCompressionAsync` after successful message processing
- Calls `recordMessage` after successful message processing

**`__tests__/app/api/webhooks/stripe/route.test.ts`**
- Returns 400 when `stripe-signature` header is absent
- Returns 400 when signature verification fails
- Returns 200 when event is processed successfully
- Returns 200 immediately (idempotent) when event ID already exists in `webhook_events`

**`__tests__/lib/env.test.ts`**
- `env` object: throws `ZodError` if any required var is missing; returns typed env object when all vars are present; `JWT_SECRET` validation rejects strings shorter than 32 chars; `STRIPE_SECRET_KEY` validation rejects strings not starting with `sk_`

**AI/LLM used:** No

**External integrations:** None (all external calls mocked via `vi.mock()`)

**Data stored:** `coverage/` directory (gitignored)

---

### 3.7 Module 7 — E2E Test Suite

**Purpose:** Write Playwright tests covering the four critical user flows against a local `next dev` server.

**Trigger:**
- Type: Module hand-off
- Specifics: After Module 6 (unit/integration tests pass)

**Target environment:** `http://localhost:3000` via `npm run dev`

**E2E test isolation:** Tests use Stripe test mode (test API keys in `.env.local`). Tests use a separate Supabase database (test project or test schema). E2E tests are not isolated via mocks — they make real HTTP calls to the local server which makes real Supabase and Stripe test calls.

**Inputs:**
| Field | Type | Source | Required? | Constraints |
|-------|------|--------|-----------|-------------|
| `playwright.config.ts` | TypeScript | project root | Yes | Configured for `localhost:3000` |
| Running local dev server | process | terminal | Yes | `npm run dev` |
| `.env.local` with test credentials | env file | project root | Yes | Must have Stripe test keys, Supabase test project |

**Outputs:**
| Field | Type | Destination | Format / Example |
|-------|------|-------------|-----------------|
| E2E test files | TypeScript | `e2e/` directory | e.g., `e2e/auth.spec.ts`, `e2e/chat.spec.ts` |

**Required E2E flows:**

**Flow 1 — Sign up + onboarding (`e2e/signup.spec.ts`)**
1. Navigate to `/signup`
2. Fill in email and password; submit
3. Verify redirect to Stripe checkout page (or to a plan selection page with Stripe checkout embedded)
4. Complete Stripe checkout with test card `4242 4242 4242 4242`
5. Verify redirect back to app and landing on the chat interface
6. Assert: user can see the chat input field; a welcome message or onboarding prompt is visible
7. Assert: `users` table row in Supabase has `subscription_status = 'active'`

**Flow 2 — Login + session persistence (`e2e/auth.spec.ts`)**
1. Navigate to `/login`
2. Enter credentials for a pre-existing test user (created in test setup)
3. Submit login form
4. Assert: redirect to chat interface
5. Assert: `risedial_session` cookie is set with `httpOnly` flag
6. Reload the page
7. Assert: still logged in (chat interface visible, not redirected to login)
8. Navigate to a protected API route directly
9. Assert: 200 response (not 401)

**Flow 3 — Chat + AI response flow (`e2e/chat.spec.ts`)**
1. Log in as the pre-existing test user (via API or UI)
2. Navigate to the chat page
3. Type a test message and submit
4. Assert: user message appears in the chat history
5. Assert: an assistant response appears within 30 seconds
6. Assert: the response is not an error message
7. Assert: both messages persist after page reload

**Flow 4 — Stripe billing portal / plan management (`e2e/billing.spec.ts`)**
1. Log in as an active subscriber test user
2. Navigate to the account or billing settings page
3. Click the "Manage subscription" or equivalent button
4. Assert: redirect to Stripe Customer Portal (URL contains `billing.stripe.com`)
5. Return to app (navigate back)
6. Assert: app shows correct subscription status

**Edge cases:**
| Scenario | System Behavior | User Notified? | Processing Continues? |
|----------|----------------|----------------|----------------------|
| Stripe checkout webhook not received during test | Test waits up to 15 seconds for `subscription_status` to update, then fails with a descriptive timeout message | No — test fails | No |
| Dev server not running | Playwright webServer config starts it automatically | No | Yes |

**AI/LLM used:** No

**Data stored:** `playwright-report/` directory (gitignored), test videos/screenshots on failure in `test-results/` (gitignored)

---

### 3.8 Module 8 — CI Pipeline

**Purpose:** Add a GitHub Actions workflow that runs type checking, unit tests, and E2E tests on every push and pull request.

**Trigger:**
- Type: Module hand-off
- Specifics: After Module 7 (all tests pass locally)

**Inputs:**
| Field | Type | Source | Required? | Constraints |
|-------|------|--------|-----------|-------------|
| All test files | TypeScript | local filesystem | Yes | Must all pass locally before CI is added |
| GitHub repository secrets | key-value | GitHub Settings > Secrets | Yes | All env vars from Area 11 must be added as repository secrets |

**Outputs:**
| Field | Type | Destination | Format / Example |
|-------|------|-------------|-----------------|
| `.github/workflows/ci.yml` | YAML | `.github/workflows/ci.yml` | GitHub Actions workflow file |

**Step-by-step process:**
1. Create `.github/workflows/ci.yml`:
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsc --noEmit

  unit-tests:
    runs-on: ubuntu-latest
    needs: typecheck
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
      STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
      STRIPE_PRICE_MONTHLY: ${{ secrets.STRIPE_PRICE_MONTHLY }}
      STRIPE_PRICE_ANNUAL: ${{ secrets.STRIPE_PRICE_ANNUAL }}
      STRIPE_PRICE_PREMIUM_MONTHLY_ADDON: ${{ secrets.STRIPE_PRICE_PREMIUM_MONTHLY_ADDON }}
      STRIPE_PRICE_PREMIUM_ANNUAL_ADDON: ${{ secrets.STRIPE_PRICE_PREMIUM_ANNUAL_ADDON }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx vitest run --coverage

  e2e-tests:
    runs-on: ubuntu-latest
    needs: unit-tests
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
      STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
      STRIPE_PRICE_MONTHLY: ${{ secrets.STRIPE_PRICE_MONTHLY }}
      STRIPE_PRICE_ANNUAL: ${{ secrets.STRIPE_PRICE_ANNUAL }}
      STRIPE_PRICE_PREMIUM_MONTHLY_ADDON: ${{ secrets.STRIPE_PRICE_PREMIUM_MONTHLY_ADDON }}
      STRIPE_PRICE_PREMIUM_ANNUAL_ADDON: ${{ secrets.STRIPE_PRICE_PREMIUM_ANNUAL_ADDON }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npx playwright test
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
```
2. Add all required env vars to GitHub repository secrets (document list for user in AUDIT.md Area 11).
3. Push to main and verify all three CI jobs pass.

**AI/LLM used:** No

**Data stored:** GitHub Actions artifacts (playwright-report on failure, 7-day retention)

---

## 4. INTEGRATION MAP

### 4.1 Module Hand-offs
| From | To | Data Passed | Timing | Condition |
|------|----|-------------|--------|-----------|
| Module 1 | Module 2 | Clean build (exit code 0) | Sync | Only when tsc + next build both pass |
| Module 2 | Module 3 | `AUDIT.md` with all 14 areas | Sync | Only when all areas are documented |
| Module 3 | Module 4 | Fixed source files committed | Sync | Only when all blocking findings are fixed |
| Module 4 | Module 5 | `lib/env.ts` and `.env.example` committed | Sync | Always |
| Module 5 | Module 6 | `vitest.config.ts`, `playwright.config.ts`, test scripts | Sync | Only when empty test runs exit 0 |
| Module 6 | Module 7 | `__tests__/` directory with 100% coverage | Sync | Only when `npx vitest run --coverage` exits 0 |
| Module 7 | Module 8 | `e2e/` directory with 4 passing flows | Sync | Only when `npx playwright test` exits 0 locally |

### 4.2 Full System Flow (End-to-End)
1. Developer deletes `tsconfig.tsbuildinfo`, runs `npx tsc --noEmit`, fixes all errors → Module 1 complete
2. Developer reads all source files against the 14-area checklist, writes `AUDIT.md` → Module 2 complete
3. Developer applies all AUDIT.md findings as atomic commits → Module 3 complete
4. Developer creates `lib/env.ts` with zod, updates all `process.env` references → Module 4 complete
5. Developer installs Vitest + Playwright, creates config files, verifies empty run passes → Module 5 complete
6. Developer writes Vitest tests for all `lib/` and `app/api/` files, achieves 100% coverage → Module 6 complete
7. Developer writes 4 Playwright flows against local dev server with Stripe test mode → Module 7 complete
8. Developer commits `.github/workflows/ci.yml`, pushes, all 3 CI jobs pass → Module 8 complete
9. Final state: green CI badge on main, `npx tsc --noEmit` exits 0, `npx vitest run --coverage` exits 0 at 100%, `npx playwright test` exits 0

### 4.3 Parallel Execution
No parallel execution — all modules run sequentially. Each module's exit gate is the entry condition for the next.

---

## 5. DATA SCHEMAS

### 5.1 users table

**Storage location:** Supabase PostgreSQL — table `users`

**Fields:**
| Field | Type | Required | Constraints | Example Value |
|-------|------|----------|-------------|---------------|
| id | uuid | Yes | PRIMARY KEY, DEFAULT gen_random_uuid() | `a1b2c3d4-...` |
| email | text | Yes | UNIQUE NOT NULL | `user@example.com` |
| password_hash | text | Yes | NOT NULL (bcryptjs hash) | `$2b$10$...` |
| preferred_name | varchar(30) | No | NULL | `"Alex"` |
| subscription_status | text | Yes | CHECK IN ('active', 'lapsed', 'cancelled') | `"active"` |
| stripe_customer_id | text | No | NULL | `"cus_abc123"` |
| stripe_subscription_id | text | No | NULL | `"sub_abc123"` |
| stripe_premium_item_id | text | No | NULL | `"si_abc123"` |
| plan_type | text | No | CHECK IN ('monthly', 'annual') | `"monthly"` |
| has_premium_memory | boolean | Yes | NOT NULL DEFAULT false | `false` |
| next_billing_date | timestamptz | No | NULL | `"2026-06-04T00:00:00Z"` |
| subscription_lapsed_at | timestamptz | No | NULL | `null` |
| created_at | timestamptz | Yes | NOT NULL DEFAULT now() | `"2026-05-04T00:00:00Z"` |
| password_reset_token | text | No | NULL | `null` |
| password_reset_expires | timestamptz | No | NULL | `null` |

**Lifecycle:**
- Created by: Sign-up API route
- Read by: Login route, middleware (via JWT), subscription gate, memory executor
- Updated by: Stripe webhook handler (subscription fields), password reset flow
- Deleted by: Never (soft lifecycle via `subscription_status`)

---

### 5.2 chats table

**Storage location:** Supabase PostgreSQL — table `chats`

**Fields:**
| Field | Type | Required | Constraints | Example Value |
|-------|------|----------|-------------|---------------|
| id | uuid | Yes | PRIMARY KEY | `b2c3d4e5-...` |
| user_id | uuid | Yes | NOT NULL REFERENCES users(id) ON DELETE CASCADE | `a1b2c3d4-...` |
| title | varchar(40) | Yes | NOT NULL | `"My first session"` |
| created_at | timestamptz | Yes | NOT NULL DEFAULT now() | `"2026-05-04T00:00:00Z"` |
| deleted_at | timestamptz | No | NULL (soft delete) | `null` |

**Lifecycle:**
- Created by: Chat creation API route
- Read by: Chat list route, message route
- Updated by: Chat rename/soft-delete route (`deleted_at`)
- Deleted by: Never (cascaded only on user delete)

---

### 5.3 messages table

**Storage location:** Supabase PostgreSQL — table `messages`

**Fields:**
| Field | Type | Required | Constraints | Example Value |
|-------|------|----------|-------------|---------------|
| id | uuid | Yes | PRIMARY KEY | |
| chat_id | uuid | Yes | NOT NULL REFERENCES chats(id) ON DELETE CASCADE | |
| role | text | Yes | CHECK IN ('user', 'assistant') | `"user"` |
| content | text | Yes | NOT NULL | `"Hello, Rise"` |
| created_at | timestamptz | Yes | NOT NULL DEFAULT now() | |
| user_message_index | integer | No | NULL — set only on user messages | `1`, `2`, `50` |

**Note:** `user_message_index` must be set on every user message insert for the memory compression trigger to work. If absent, compression never fires.

**Lifecycle:**
- Created by: Message route (`app/api/chat/[chatId]/message/route.ts`)
- Read by: Memory compression trigger (`checkCompressionTrigger`)
- Updated by: Never
- Deleted by: Cascade on chat delete

---

### 5.4 memory_profiles table

**Storage location:** Supabase PostgreSQL — table `memory_profiles`

**Fields:**
| Field | Type | Required | Constraints | Example Value |
|-------|------|----------|-------------|---------------|
| id | uuid | Yes | PRIMARY KEY | |
| user_id | uuid | Yes | NOT NULL UNIQUE REFERENCES users(id) | |
| profile_json | jsonb | Yes | NOT NULL | `{"traits": [...], "goals": [...]}` |
| source_chats | jsonb | Yes | NOT NULL DEFAULT '[]' | `["chat-id-1", "chat-id-2"]` |
| version | integer | Yes | NOT NULL DEFAULT 1 | `3` |
| generated_at | timestamptz | Yes | NOT NULL DEFAULT now() | |
| last_updated_at | timestamptz | Yes | NOT NULL DEFAULT now() | |
| model_used | varchar(50) | Yes | NOT NULL | `"gpt-4o"` |

**Lifecycle:**
- Created by: `generateInitialProfile` (Module 8 in the app — memory compression)
- Read by: Chat message route (context injection), compression patch
- Updated by: `patchMemoryProfile` (increments version, updates profile_json)
- Deleted by: Cascade on user delete

---

### 5.5 rate_limit_tracking table

**Storage location:** Supabase PostgreSQL — table `rate_limit_tracking`

**Fields:**
| Field | Type | Required | Constraints | Example Value |
|-------|------|----------|-------------|---------------|
| id | uuid | Yes | PRIMARY KEY | |
| user_id | uuid | Yes | NOT NULL REFERENCES users(id) ON DELETE CASCADE | |
| window_start | timestamptz | Yes | NOT NULL | `"2026-05-04T10:00:00Z"` |
| message_count | integer | Yes | NOT NULL DEFAULT 0 | `43` |

**Lifecycle:**
- Created by: `recordMessage` when no active window exists
- Read by: `checkRateLimit`, `recordMessage`
- Updated by: `increment_message_count` RPC (atomic increment)
- Deleted by: Never (old windows accumulate; query filters by `window_start >= now() - 60 minutes`)

---

### 5.6 webhook_events table

**Storage location:** Supabase PostgreSQL — table `webhook_events`

**Fields:**
| Field | Type | Required | Constraints | Example Value |
|-------|------|----------|-------------|---------------|
| id | uuid | Yes | PRIMARY KEY | |
| stripe_event_id | text | Yes | UNIQUE NOT NULL | `"evt_abc123"` |
| event_type | text | Yes | NOT NULL | `"checkout.session.completed"` |
| processed_at | timestamptz | Yes | NOT NULL DEFAULT now() | |
| payload | jsonb | Yes | NOT NULL | Full Stripe event object |

**Lifecycle:**
- Created by: Stripe webhook route (inserted BEFORE event processing for crash safety)
- Read by: Stripe webhook route (idempotency check)
- Updated by: Never
- Deleted by: Never

---

## 6. AI/LLM SPECIFICATIONS

The audit/fix/test pipeline itself (Modules 1–8) uses no LLM calls. The RiseDial application's AI integrations are documented here as audit targets that the test suite must verify.

### 6.1 Chat Response — gpt-4o-mini

**Used in:** App module — `lib/openai/client.ts` → `callRise`  
**Model:** `gpt-4o-mini`  
**Task type:** Generate text (AI coaching response)

**What the audit must verify:**
- `callRise` passes the correct message array format (OpenAI `ChatCompletionMessageParam[]`)
- The system prompt from `lib/rise/` is injected correctly as the first `system` role message
- Any memory profile from `memory_profiles` is injected into the system prompt context
- The function handles OpenAI API errors (network failure, rate limit, model error) and returns a meaningful error to the route handler

**On failure:** The route handler must catch the error and return a 500 or a user-visible error message. Silent failure that results in an empty assistant message is not acceptable.

---

### 6.2 Memory Compression — gpt-4o / gpt-4o-mini

**Used in:** App module — `lib/memory/compress.ts` → `generateInitialProfile`, `lib/memory/patch.ts` → `patchMemoryProfile`  
**Model:** `gpt-4o` for premium users, `gpt-4o-mini` for standard users  
**Task type:** Extract structured data (psychological profile as JSON)

**What the audit must verify:**
- Both functions request JSON mode from OpenAI (`response_format: { type: 'json_object' }`)
- The returned JSON is parsed with `JSON.parse()` in a try/catch — malformed JSON must not crash the process
- The parsed JSON is validated to have the expected fields before being upserted to `memory_profiles`
- The upsert uses `user_id` as the conflict target with `onConflict: 'user_id'`

**On failure:** `executeCompressionAsync` has a 3-attempt retry with exponential backoff and swallows all errors (fire-and-forget). The audit must verify no exception propagates to the HTTP response.

---

## 7. CONSTRAINTS AND REQUIREMENTS

### 7.1 Technology Decisions
| Decision | Choice | Status | Reason |
|----------|--------|--------|--------|
| Framework | Next.js 14 App Router | Fixed | Pre-existing codebase |
| Database | Supabase (PostgreSQL, service role) | Fixed | Pre-existing |
| Auth | Custom HS256 JWT via `crypto.subtle` | Fixed | Pre-existing |
| Payments | Stripe SDK v22, API version `2026-04-22.dahlia` | Fixed | Already upgraded |
| AI provider | OpenAI (`gpt-4o-mini`, `gpt-4o`) | Fixed | Pre-existing |
| Styling | Tailwind CSS 3 | Fixed | Pre-existing |
| Hosting | Vercel | Fixed | Pre-existing |
| Unit/integration tests | Vitest + `@testing-library/react` | Fixed | User confirmed |
| E2E tests | Playwright | Fixed | User confirmed |
| Test isolation | `vi.mock()` at module boundary | Fixed | User confirmed |
| Coverage target | 100% lines/functions/branches for `lib/` + `app/api/` | Fixed | User confirmed |
| E2E target | `localhost:3000` via `npm run dev` | Fixed | User confirmed |
| CI | GitHub Actions | Fixed | User confirmed |
| Env validation | zod (already in `package.json`) | Fixed | User confirmed |

### 7.2 Performance Requirements
No hard performance requirements defined for the audit/fix/test pipeline itself. The application has no documented SLA.

### 7.3 Hard Constraints
- No new product features may be added during this pipeline — scope is limited to correctness, not functionality
- The Supabase schema (`001_initial_schema.sql`) is accepted as the ground truth; no schema changes except the `increment_message_count` RPC function for atomic rate limiting
- All fixes must preserve existing API contract shapes (no breaking changes to request/response formats)
- `jsonwebtoken` may be removed from `package.json` only after confirming it has zero remaining imports in the codebase

---

## 8. BUILD ORDER

| # | Module | One-Line Description | Dependencies |
|---|--------|----------------------|-------------|
| 1 | Build Error Resolution | Delete stale tsbuildinfo, fix all tsc errors, confirm next build passes | None |
| 2 | Codebase Audit | 14-area read-only review producing AUDIT.md | Requires Module 1 |
| 3 | Code Fixes | Apply all AUDIT.md findings as atomic commits | Requires Module 2 |
| 4 | Environment Variable Validation | Create lib/env.ts with zod; update all process.env references | Requires Module 3 |
| 5 | Test Infrastructure | Install Vitest + Playwright; create config files; verify empty runs pass | Requires Module 4 |
| 6 | Unit & Integration Test Suite | Write Vitest tests for 100% coverage of lib/ + app/api/ | Requires Module 5 |
| 7 | E2E Test Suite | Write 4 Playwright flows against local dev server | Requires Module 6 |
| 8 | CI Pipeline | Add GitHub Actions ci.yml; push; verify all 3 jobs pass | Requires Module 7 |

### 8.1 Integration Checkpoints
| After Completing | Wire To | Verify By |
|-----------------|---------|-----------|
| Module 1 | Module 2 | `npx tsc --noEmit` exits 0; `next build` exits 0 |
| Module 2 | Module 3 | `AUDIT.md` exists in project root with all 14 sections populated |
| Module 3 | Module 4 | `npx tsc --noEmit` still exits 0 after all fixes |
| Module 4 | Module 5 | App starts (`npm run dev`) without throwing an env validation error when `.env.local` is present |
| Module 5 | Module 6 | `npx vitest run` exits 0 on empty test suite; `npx playwright test` exits 0 on empty `e2e/` directory |
| Module 6 | Module 7 | `npx vitest run --coverage` exits 0; coverage report shows 100% for all included files |
| Module 7 | Module 8 | `npx playwright test` exits 0 for all 4 flows against local dev server |
| Module 8 | Done | GitHub Actions CI shows all green on latest main branch push |

---

## 9. SUCCESS CRITERIA

| # | Test Description | Pass Condition | Fail Condition |
|---|-----------------|---------------|----------------|
| 1 | TypeScript compilation | `npx tsc --noEmit` exits with code 0, zero output | Any error or warning output |
| 2 | Next.js build | `next build` completes without error on Vercel | Build log shows any error |
| 3 | Unit test pass | `npx vitest run` exits 0 | Any test fails or throws |
| 4 | 100% coverage | `npx vitest run --coverage` shows 100% lines/functions/branches for `lib/**` and `app/api/**` | Any file below 100% |
| 5 | E2E sign-up flow | Playwright: new user signs up, pays, lands in chat | Test times out, gets error page, or `subscription_status` is not `active` |
| 6 | E2E login + session | Playwright: user logs in, `risedial_session` cookie set, reload stays logged in | Cookie absent, or page redirects to login after reload |
| 7 | E2E chat flow | Playwright: message sent, AI response appears within 30s, both persist after reload | No response within 30s, or error message appears, or messages not in DB |
| 8 | E2E billing portal | Playwright: billing button redirects to `billing.stripe.com` | No redirect, or 404, or wrong URL domain |
| 9 | Env validation fail-fast | Starting the app without `JWT_SECRET` set throws: `ZodError: JWT_SECRET must be at least 32 characters` | App starts silently or throws a generic error without identifying the missing var |
| 10 | Webhook idempotency | Sending the same Stripe event ID twice to `/api/webhooks/stripe` returns 200 on both calls but only inserts one row in `webhook_events` | Second call returns non-200, or two rows inserted |
| 11 | Rate limiter blocks at 60 | After 60 messages in 60 minutes, the 61st returns 429 | 61st message succeeds, or rate limiter throws instead of returning 429 |
| 12 | Memory trigger fires at 50 | After 50 user messages in a chat, `memory_profiles` row is created for the user | No row created, or row created at wrong count |
| 13 | GitHub CI all green | Pushing to main triggers CI; all 3 jobs (typecheck, unit-tests, e2e-tests) complete with green check | Any job fails on a clean push to main |
| 14 | Dead code absent | Grep for `PREMIUM_PRODUCT_ID`, `recordRateLimitMessage`, `createServerClient` returns zero results | Any match found |

### 9.1 End-to-End Smoke Test

1. Developer clones the repo, copies `.env.example` to `.env.local`, and fills in real Stripe test credentials and Supabase test project credentials.
2. Developer runs `npm install` — exits 0.
3. Developer runs `npx tsc --noEmit` — exits 0, zero output.
4. Developer runs `npm run dev` — server starts at `localhost:3000` without throwing any env validation error.
5. Developer opens `localhost:3000/signup` in a browser, enters an email and password, and submits.
6. Browser redirects to a Stripe Checkout page.
7. Developer enters Stripe test card `4242 4242 4242 4242`, any future expiry, any CVC.
8. Stripe redirects back to the app; developer lands on the chat interface.
9. Developer types "Hello" and hits Send.
10. An AI response appears in the chat within 30 seconds.
11. Developer navigates to account/billing settings and clicks "Manage subscription."
12. Browser opens a `billing.stripe.com` page.
13. Developer runs `npx vitest run --coverage` in a separate terminal — exits 0, all files at 100%.
14. Developer runs `npx playwright test` — exits 0, 4/4 flows pass.
15. Developer pushes to GitHub — CI pipeline shows 3 green checks within 10 minutes.
16. Final observable state: green CI badge on main, zero TypeScript errors, 100% test coverage, 4 E2E flows passing.

---

## 10. OPEN DECISIONS

| Decision | Options | Recommendation |
|----------|---------|----------------|
| Supabase project for E2E tests | Reuse live project with test data / Create a separate test Supabase project | Create a separate Supabase project named `risedial-test` to isolate test data from production |
| Rate limiter RPC migration file | Add as `002_atomic_rate_limit.sql` in `supabase/migrations/` / Apply directly via Supabase dashboard | Add as a migration file so it's tracked in source control |
| `orchestration/` directory | Include in TypeScript compilation / Exclude via tsconfig | Exclude by adding `"orchestration"` to tsconfig `exclude` array — it is context-only, not app code |
| `jsonwebtoken` + `@types/jsonwebtoken` removal | Remove immediately in Module 3 if zero imports found / Leave as harmless dependency | Remove both in Module 3 if `grep -r "jsonwebtoken" --include="*.ts"` returns no results outside `node_modules` |
